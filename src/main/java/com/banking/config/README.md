# Config Layer

Chứa toàn bộ cấu hình hệ thống:

- Database config (Datasource, JPA, Hibernate)
- Security config (JWT, filter)
- Transaction config

Không chứa business logic.
Chỉ phục vụ khởi tạo và cấu hình hệ thống.